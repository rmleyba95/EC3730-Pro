Contents
---------
|-Archive
|-Report
  |-figures
  |-logs
  |-Final Project_v5.synctex.gz
|-Virtual Machines
  |-Required APK's
|-EC3730_Final_Project.pdf

Archive contains documents that were used for initial setup and old lab reports. Nothing in archives is required for future development. 

Report contains all latex files and figures required to generate the pdf report. It also contains a folder named logs, that folder contains the zipped contents from each router with all configuration logs. There is also a scripts folder located in logs, this was the script created to extract the data off each node. 

Virtual Machines does not contain the virtual machines since they are too large to put on GitHub. They will be located on one drive and shared in the teams chat. This folder does contain Required APK's which was used to set up the routers on VM. It also contains an image with the password to the LucI gui for each router. 

username: root
LucI password: Su123!
ssh password: password 

Misc: gitignore file is used to remove larger files from upload. Please disregard. 